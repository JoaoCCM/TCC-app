import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    modalToggle: {
        alignSelf: "flex-end",
    },
    modalContent: {
        paddingVertical: 15,
        paddingHorizontal: 0,
        paddingBottom: 0,
    },
});

export default styles;
